# Label data

`curb_ramp_labels.csv` is the working human-label file. The generator refuses to
overwrite it after creation so completed labels cannot be erased accidentally.

The image identifiers and paths will be populated after Mapillary collection. Follow
`docs/labeling-guide.md` and retain uncertain cases as `cannot_determine`.
